spring:
  main:
    allow-bean-definition-overriding: true
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/cvix_test
    username: test
    password: test
  liquibase:
    change-log: classpath:/db/changelog/identity-master.yaml
  security:
    oauth2:
      client:
        registration:
          oidc:
            client-secret: web_app
            client-id: web_app
            scope: openid,profile,email
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          oidc:
            issuer-uri: https://cvix.com/realms/cvix
            authorization-uri: ${application.security.oauth2.issuer-uri}/protocol/openid-connect/auth
            token-uri: ${application.security.oauth2.issuer-uri}/protocol/openid-connect/token
            user-info-uri: ${application.security.oauth2.issuer-uri}/protocol/openid-connect/userinfo
            jwk-set-uri: ${application.security.oauth2.issuer-uri}/protocol/openid-connect/certs
            user-name-attribute: preferred_username

application:
  security:
    oauth2:
      audience:
        - account
        - web_app

testing:
  security:
    username: "john.doe@profiletailors.com"
    password: "S3cr3tP@ssw0rd*123"
resume:
  pdf:
    docker:
      timeout-seconds: 300
