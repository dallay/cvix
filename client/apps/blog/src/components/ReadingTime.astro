---
import { cn } from "@cvix/lib";
import type { HTMLAttributes } from "astro/types";
import { type Lang, useTranslations } from "@/i18n/i18n.ts";

interface Props extends HTMLAttributes<"span"> {
	readingTime: string;
}
const { readingTime, ...attrs } = Astro.props;

const currentLocale = Astro.currentLocale as Lang;
const t = useTranslations(currentLocale);
---

{
	readingTime && (
		<span
			class={
				cn("text-gray-500 dark:text-gray-400 font-inter text-[9px] leading-[140%] tracking-widest uppercase rounded gap-1", attrs.class)
			}
			aria-label={t("post.reading.time.aria", { time: readingTime })}
		>
			{t("post.reading.time", { time: readingTime })}
		</span>
	)
}
