meta {
  name: waitlist
}

auth {
  mode: inherit
}

docs {
  # ============================================================
  # Bruno folder.bru | Waitlist endpoints folder
  # This file serves as a folder for organizing all endpoints
  # related to waitlist functionality. It inherits authentication
  # settings for all included endpoints. No actual API endpoint is
  # defined here. Use this as a container only.
  # ============================================================
}
