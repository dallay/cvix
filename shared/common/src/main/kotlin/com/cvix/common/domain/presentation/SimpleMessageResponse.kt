package com.cvix.common.domain.presentation
/**
 * A simple message response used for returning messages in API responses
 * @param message the message to return
 */
data class SimpleMessageResponse(val message: String)
