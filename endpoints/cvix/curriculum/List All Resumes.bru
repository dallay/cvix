meta {
  name: List All Resumes
  type: http
  seq: 3
}

get {
  url: {{url}}/api/resume?workspaceId={{workspaceId}}
  body: none
  auth: inherit
}

params:query {
  workspaceId: {{workspaceId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
