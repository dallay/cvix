package com.loomify.resume.domain

/**
 * Enum representing different types of documents related to resumes.
 */
enum class DocumentType(val type: String) {
    RESUME("resume"),
    COVER_LETTER("cover_letter"),
}
