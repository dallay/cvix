---
const templates = [
  { name: "Executive", color: "bg-stone-100" },
  { name: "Creative", color: "bg-rose-50" },
  { name: "Minimal", color: "bg-white" },
  { name: "Modern", color: "bg-slate-50" },
];
---

<section class="py-24 bg-secondary/30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-serif font-bold text-foreground mb-4">
        Beautiful and unique <br /> resumes right out of the box
      </h2>
      <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
        Pick a template, enter your content, and get a professional resume in minutes.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {
        templates.map((template) => (
          <div class="group relative aspect-[1/1.4] rounded-lg shadow-lg overflow-hidden transition-transform hover:-translate-y-2 duration-300">
            <div class={`w-full h-full ${template.color} p-4 flex flex-col text-slate-900`}>
              <div class="h-4 w-1/3 bg-current opacity-10 rounded mb-4" />
              <div class="h-2 w-full bg-current opacity-5 rounded mb-2" />
              <div class="h-2 w-full bg-current opacity-5 rounded mb-2" />
              <div class="h-2 w-2/3 bg-current opacity-5 rounded mb-8" />

              <div class="grid grid-cols-3 gap-4 mb-4">
                <div class="col-span-1 h-20 bg-current opacity-5 rounded" />
                <div class="col-span-2 space-y-2">
                  <div class="h-2 w-full bg-current opacity-5 rounded" />
                  <div class="h-2 w-full bg-current opacity-5 rounded" />
                  <div class="h-2 w-full bg-current opacity-5 rounded" />
                </div>
              </div>
              <div class="space-y-2 mt-auto">
                <div class="h-2 w-full bg-current opacity-5 rounded" />
                <div class="h-2 w-full bg-current opacity-5 rounded" />
                <div class="h-2 w-full bg-current opacity-5 rounded" />
              </div>
            </div>
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/5 transition-colors flex items-center justify-center opacity-0 group-hover:opacity-100">
              <span class="bg-primary text-primary-foreground px-4 py-2 rounded-full font-medium shadow-lg transform translate-y-4 group-hover:translate-y-0 transition-all duration-300">
                Use {template.name}
              </span>
            </div>
          </div>
        ))
      }
    </div>

    <div class="mt-12 text-center">
      <a
        href="#"
        class="inline-flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-full text-white bg-primary hover:bg-primary/90 md:py-4 md:text-lg md:px-10 transition-all shadow-lg hover:shadow-xl"
      >
        View all templates
      </a>
    </div>
  </div>
</section>
